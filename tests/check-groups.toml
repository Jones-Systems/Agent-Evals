schema = "agent-evals-check-groups/v1"

[groups.portable-core]
summary = "Portable core compatibility and deterministic grading"
modules = ["test_core"]

[groups.campaign]
summary = "Deterministic campaign compilation and requested profiles"
modules = ["test_campaign"]

[groups.publication-analysis]
summary = "Immutable atomic storage, sealing, closed public projection, and blinded analysis bindings"
modules = ["test_object_store", "test_publication"]

[groups.offline-cli]
summary = "Closed-JSON offline CLI, conformance, import boundaries, and installed-wheel execution"
modules = ["test_cli"]

[groups.public-readiness]
summary = "Public licensing, contribution guidance, and GitHub-hosted CI trust boundary"
modules = ["test_public_readiness"]
